name: Haskell CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    # - name: Setup Haskell
    #   uses: haskell/actions/setup@v1
    #   with:
    #     ghc-version: '8.10.4'
    # - name: Install C lib
    #   run: sudo apt-get update && sudo apt install python3-pip python3-numpy libgtk2.0-dev libcairo2-dev libpango1.0-dev libglib2.0-dev libx11-dev libgl1-mesa-dev libglu1-mesa-dev freeglut3-dev
    # - name: Build
    #   run: cabal build
      

    - name: Установка Docker
      run: apt-get update && \
          apt-get install -y apt-transport-https ca-certificates curl gnupg-agent software-properties-common && \
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg | apt-key add - && \
          add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" && \
          apt-get update && \
          apt-get install -y docker-ce docker-ce-cli containerd.io
      
    - name: Настройка доступа к Docker внутри контейнера
      run: groupadd -g 999 docker && \
          usermod -aG docker root
      
    - name: Команда для сборки образа и загрузки его в репозиторий DockerHub
      run: docker build -t NBodyProblem . && docker push NBodyProblem